[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
pandas = ">=2.2.0"
numpy = ">=1.26.0"
scipy = ">=1.11.0"
statsmodels = ">=0.14.0"
matplotlib = ">=3.8.0"
jinja2 = ">=3.1.0"
pyarrow = ">=15.0.0"
python-dateutil = ">=2.9.0"
pytest = ">=7.4.0"
pyyaml = ">=6.0"
seaborn = ">=0.13.0"

[dev-packages]

[requires]
python_version = "3.10"
