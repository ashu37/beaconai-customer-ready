services:
  - type: web
    name: beaconai-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      # The least-privilege application role (docs/SECURITY_HARDENING_PLAN.md, PR A runbook).
      - key: DATABASE_URL
        sync: false
      # The owner connection, for schema changes, grants and policies.
      - key: MIGRATION_DATABASE_URL
        sync: false
      - key: TOKEN_ENCRYPTION_SECRET
        generateValue: true
      # Signs sessions only; must differ from TOKEN_ENCRYPTION_SECRET.
      - key: SESSION_SECRET
        generateValue: true
      - key: BEACONAI_ADMIN_TOKEN
        sync: false
      - key: API_BASE_URL
        value: https://beaconai-app-kvre.onrender.com/api
      - key: WEB_BASE_URL
        value: https://beaconai-app-kvre.onrender.com
      - key: SHOPIFY_CLIENT_ID
        sync: false
      - key: SHOPIFY_CLIENT_SECRET
        sync: false
      - key: SHOPIFY_SCOPES
        value: read_products,read_customers,read_orders
      - key: KLAVIYO_CLIENT_ID
        sync: false
      - key: KLAVIYO_CLIENT_SECRET
        sync: false
      - key: KLAVIYO_SCOPES
        value: accounts:read campaigns:read campaigns:write catalogs:read flows:read lists:read lists:write profiles:read profiles:write segments:read templates:read templates:write
      - key: KLAVIYO_REVISION
        value: 2026-04-15
