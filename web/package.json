{
  "name": "beaconai-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5173",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 5173",
    "test": "npm run lint && node --import ./tests/register.mjs --test \"tests/**/*.test.js\"",
    "lint": "eslint src tests"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "react": "latest",
    "react-dom": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@testing-library/react": "^16.3.3",
    "esbuild": "^0.28.2",
    "eslint": "^10.10.0",
    "jsdom": "^29.1.1"
  }
}
