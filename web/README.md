# BeaconAI Web

React/Vite MVP dashboard for BeaconAI.

## Run locally

```bash
cd web
npm install
cp .env.example .env
npm run dev
```

Open http://localhost:5173

The API must be running on http://localhost:4000.
